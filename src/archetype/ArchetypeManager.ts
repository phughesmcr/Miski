import type { Component } from "../component/Component.ts";
import type { SchemaOrNull } from "../types.ts";

export class ArchetypeManager {
  constructor(capacity: number, components: Component<SchemaOrNull>[]) {
  }
}
