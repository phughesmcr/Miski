{
  "presets": [
    ["@babel/preset-env", {
      "targets": "defaults, not safari < 14, not iOS_saf < 14.3, not Baidu >= 0, not Blackberry >= 0, not ie >= 0, not ie_mob >= 0, not OperaMobile >= 0, not UCAndroid >= 0, not kaios >= 0, not QQAndroid >= 0",
      "bugfixes": true,
      "corejs": {
        "version": "3.12",
        "proposals": true
      },
      "useBuiltIns": "usage"
    }],
    "@babel/preset-typescript"
  ],
  "plugins": [
    "@babel/plugin-proposal-class-properties",
    "@babel/plugin-proposal-private-property-in-object",
    "@babel/proposal-object-rest-spread",
    "@babel/plugin-syntax-bigint"
  ]
}
